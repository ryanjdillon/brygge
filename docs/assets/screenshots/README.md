# README screenshots

The root `README.md` embeds four screenshots from this directory. Add real PNGs at these exact paths — until they exist, the README shows broken-image placeholders on GitHub:

| File | Suggested content | Width |
|------|-------------------|-------|
| `dashboard.png` | Admin landing / overview after login | ~1400px, 16:10 |
| `faktura.png` | Faktura page (a populated invoice list or the create form) | ~1400px, 16:10 |
| `harbor-map.png` | Interactive harbour map with slips | ~1400px, 16:10 |
| `inbox.png` | Shared inbox at `/admin/inbox` with a thread open | ~1400px, 16:10 |

Capture at a consistent window size with no personal data visible (use seeded demo data or redact). Keep each file under ~500 KB — run through `oxipng` or `pngquant` if larger. The README scales them to 48% width side-by-side, so landscape framing reads best.
